apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-server
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
  hostnames:
    - infra.xmple.io
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /argocd
    - backendRefs:
        - name: argocd-server
          port: 80
          kind: Service
